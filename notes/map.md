# Map — workspace master index

One line per file and folder. The table of contents for the whole workspace. Read this first.

_Auto-maintained. Populated on the first run, when the agent reads everything in `context/`._
